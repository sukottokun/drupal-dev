{{ attach_library('module_hero/hero-style') }}
<h4 class="animate__animated animate__bounce">{{ title }}</h4>
<ol>
    {% for item in items %}
        <li>{{ item.name }}</li>
    {% endfor %}
</ol>
<strong>This is a custom twig file.</strong>