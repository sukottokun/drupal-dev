{{ attach_library('module_hero/hero-style') }}
<h4>{{ title }}</h4>
<ol>
    {% for item in items %}
        <li class="animate__animated {{ random(['animate__bounce', 'animate__headShake','animate__jello', 'animate_tada']) }}">{{ item.name }}</li>
    {% endfor %}
</ol>
<strong>This is a custom twig file.</strong>